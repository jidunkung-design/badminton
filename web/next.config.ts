import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Keep the development badge from covering mobile navigation targets.
  devIndicators: false,
};

export default nextConfig;
